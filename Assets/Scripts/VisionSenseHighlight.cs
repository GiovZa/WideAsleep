using System.Collections;
using System.Collections.Generic;
using UnityEngine;

[RequireComponent(typeof(Interactable))]
public class VisionSenseHighlight : MonoBehaviour
{
    // This component is used as a marker for objects that can be highlighted by the VisionSense ability.
    // Ensure the object also has a component that inherits from 'Interactable'.
}
